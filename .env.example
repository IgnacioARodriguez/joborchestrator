# Copy this file to .env and fill the values.
# .env is ignored by Git and is loaded automatically by the local Python API,
# local worker, and maintenance scripts.

# Remote durable storage used by Vercel/v0 and the local worker.
TURSO_DATABASE_URL=libsql://your-database.turso.io
TURSO_AUTH_TOKEN=your-turso-token

# LLM keys used for CV import, ranking and generated materials.
NVIDIA_API_KEY=nvapi-your-key
OPENAI_API_KEY=

# Optional model overrides. Leave empty to use app defaults.
NVIDIA_BASE_URL=
NVIDIA_MODEL=
NVIDIA_RANKING_MODEL=
PROFILE_EXTRACTION_MODEL=
PROFILE_EXTRACTION_TIMEOUT_SECONDS=180
PROFILE_EXTRACTION_VALIDATION_RETRIES=1
OPENAI_MODEL=
OPENAI_RANKING_MODEL=
OPENAI_MATERIALS_MODEL=
OPENAI_MATERIALS_VALIDATION_RETRIES=1

# Optional local database path when Turso is not configured.
JOB_ORCHESTRATOR_DB_PATH=

# Optional worker tuning.
JOB_WORKER_POLL_SECONDS=5
JOB_WORKER_STALE_SECONDS=3600
RANKING_WORKER_POLL_SECONDS=5
RANKING_WORKER_CHUNK_SIZE=25
RANKING_WORKER_STALE_SECONDS=60
ALLOW_API_RANKING_RUN_ONCE=0
APPLICATION_BROWSER_HEADLESS=1
APPLICATION_BROWSER_TIMEOUT_MS=30000
APPLICATION_BROWSER_PROFILE_DIR=
ALLOW_PLAINTEXT_CREDENTIAL_STORE=0
NVIDIA_RANKING_BATCH_SIZE=2
NVIDIA_RANKING_MAX_CONCURRENCY=1
NVIDIA_RANKING_MAX_TOKENS=8192
NVIDIA_RANKING_TIMEOUT_SECONDS=180
NVIDIA_MATERIALS_TIMEOUT_SECONDS=300
NVIDIA_MATERIALS_MAX_TOKENS=8000
NVIDIA_RANKING_VALIDATION_RETRIES=1
