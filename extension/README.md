# Job Orchestrator browser extension

Manifest V3 assisted autofill scaffold. This phase includes TypeScript source for Greenhouse only.

It never clicks a submit button. The final submission action remains manual; `submission-observer.ts` only reacts after a confirmation page is detected.

Before loading in Chrome, compile the TypeScript files to the JS paths referenced by `manifest.json`.
